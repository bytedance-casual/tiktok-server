package db

import (
	"testing"
	"tiktok-server/internal/conf"
)

func TestUpdate(t *testing.T) {
	conf.Init()
	Init()

	//ctx := context.Background()
	//err := UpdateVideoFavorite(7, false, ctx)
	//assert.NoError(t, err)
}
